server {
  listen 80;
  server_name jupiter.local;

  location /dashboard/ {
    proxy_pass http://dashboard:8080/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
